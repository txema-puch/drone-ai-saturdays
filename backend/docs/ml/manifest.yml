# ML lifecycle manifest — single source of truth for the project's ML phase state.
# Lives at backend/docs/ml/manifest.yml (project convention; CLAUDE.md documents the path).
# Read by /ml-lifecycle and /develop to know which phase we're in and which gates passed.

project_name: "Drone Trajectory Anomaly Detection (LEMD)"
problem_type: "anomaly_detection"
model_track: "dl"   # tentative — LSTM Autoencoder primary, Isolation Forest baseline.
                    # Confirmed in Phase 6 once we see whether the LSTM AE meaningfully
                    # beats IF. If not, we flip to "ml" and ship IF.

current_phase: "deploy"   # Phase 7 (eval) passed + TEST BURNED 2026-06-02 (issue #29). Ship the
                          # LSTM-AE (dl confirmed on the pre-registered real-anomaly criterion).
                          # Synthetic AUROC>0.85 NOT met (0.731), but AE beats kNN on real anomalies
                          # and matches SADAR (ROC 0.667 vs 0.659). Phase 8 = course-demo only.
                          # See gates.eval.summary_phase7 + 07-eval.md.

# Gate ledger — append, never rewrite.
gates:
  problem:
    status: "passed"
    gate_semantics: "one_shot"
    passed_at: "2026-05-07"
    artifact: "backend/docs/ml/01-problem.md"
    summary: "Anomaly detection on LEMD ADS-B. LSTM AE primary, IF baseline. AUROC>0.85 primary, F2 operational, FPR<=15% guardrail. FN dominates operationally."
  data:
    status: "passed"
    gate_semantics: "cyclic"   # data accumulates per Supabase 500MB-cycle (see workflow doc).
                               # `passed` means audit discipline operational; future cycles append.
    passed_at: "2026-05-11"
    artifact: "backend/docs/ml/02-data.md"
    dataset_hash:
      # Cycle 1: 2025-03-10 to 2025-03-14 (5 days, Mon-Fri). Snapshot 2026-05-10.
      # Canonical artifact is the deduped parquet (raw had 37.5% script-rerun
      # duplicates; full audit lineage in 02-data.md).
      lemd_20250310_to_20250314__deduped_2026-05-10: "8256c65f95135597f3db07413941380fc2a0c6bbfc429b07b12b10478f7e2c10"
      # Cycle 2: 2026-03-10 to 2026-03-14 (5 days, Tue-Sat). Snapshot 2026-05-11.
      # Year-over-year repeat of cycle 1's calendar window. Snapshot is canonical
      # (no dedup needed — Monica's UNIQUE constraint working; full audit lineage
      # in 02-data.md "Cycle 2" section).
      lemd_20260310_to_20260314__snapshot_2026-05-11: "16f1bd2cbdbd519ce7bde6fbbc8df5012b188b54c5598bffc310cef34b0c6899"
      # Cycle 3: OpenSky scientific dataset (entry #1, Weekly 24h State Vectors
      # 2017-2022). 20 Mondays sampled 2017-06 → 2020-03, COVID excluded. Per-Monday
      # parquets in data/raw/opensky_states/. Combined hash is a sorted Merkle
      # of per-file sha256 values (18 of 20 planned Mondays landed; 2018-04-02
      # and 2019-12-02 failed a residual lat/lon coercion bug — documented as
      # a known limitation, ship 18). Pivot rationale: D-007.
      lemd_opensky_states_2017to2020__cycle3_2026-05-23: "98e38ba5802816a97f17b2086df18570c6f81311d80faeed0492ad87abd662e4"
    summary: |
      Audit discipline operational (cyclic gate). Future cycles append to
      dataset_hash and to 02-data.md's snapshot log without re-passing the gate.

      Cycle 1 (2026-05-10): 2025-03-10 to 2025-03-14 (5 days, Mon-Fri). 1,834,084
      raw rows → 1,146,231 deduped (37.5% removed; upstream script-rerun bug,
      fixed for future cycles via unique constraint). 1,285 unique trajectories,
      330 unique aircraft. Real=PASS, Usable=PASS, Enough=SOFT_DEV.

      Cycle 2 (2026-05-11): 2026-03-10 to 2026-03-14 (5 days, Tue-Sat).
      1,774,859 rows, 1,426 trajectories, 350 unique aircraft. ZERO duplicates
      — Monica's UNIQUE constraint fix from issue #13 confirmed effective in
      production. 3 baroaltitude nulls (0.000169%, response D, below D-206's
      0.1% tolerance). Pipeline consistency PASS. Cycle 2 surfaced one new
      methodology gap: table arrived without indexes — added (time, icao24)
      index during audit; response F (Monica's pipeline should provision
      indexes upfront in future). Real=PASS, Usable=PASS (after D-206
      tolerance), Enough=SOFT_DEV.

      Cumulative through cycle 2: 10 days (Mon-Sat coverage 6/7, Sunday still
      missing), 2,711 trajectories. Still below 30-day / 5K-trajectory
      thresholds for Enough=PASS at that point.

      Cycle 3 (closed 2026-05-23): pivot to OpenSky scientific dataset
      (entry #1) per D-007 — Trino + Supabase ingestion was the dominant
      bottleneck. 18 of 20 planned Mondays landed (2 residual failures on
      2018-04-02 and 2019-12-02 from an object-dtype-after-concat bug in
      `apply_derivations`, documented and deferred). 10s resolution (vs
      Trino path's 5s), Filter B applied (min_dist<10km AND min_alt<3km
      per trajectory) to exclude cruise overflights since the scientific
      dataset lacks the `flights_data4` origin/destination metadata the
      Trino path used. Final yield: 19,057 trajectories / 3.43M rows /
      ~123 MB on disk. Dataset is cite-able via OpenSky publication,
      which becomes a writeup asset.

      Cumulative through cycle 3: 28 days (Mon-Sat coverage 6/7, Sunday
      still missing, Monday heavily over-represented), 21,768 trajectories,
      ~6.35M rows. Crosses both the 30-day calendar threshold (with the
      caveat that cycle 3's 18 days span 2017-2020 rather than being
      contiguous) and the 5K-trajectory threshold → Enough=CONDITIONAL.
  preprocess:
    status: "passed"
    gate_semantics: "one_shot"
    passed_at: "2026-06-01"
    artifact: "backend/docs/ml/03-preprocess.md"
    pipeline_definition: "sort_segment_3min + filter_d + stage1_bounds + idle_trim + interpolate_mask + standardscaler(train) + pad_mask"
    summary: |
      Phase 3 closed 2026-06-01 (issue #22). Unfitted, segment-level preprocessing
      pipeline in backend/core/preprocessing.py (model unit = SEGMENT, gap-split at
      > 3 min). NO split, NO .fit() — the StandardScaler fit, sequence length T, and
      the train/val/test split are Phase 6 on TRAIN only (the fit/transform firewall,
      guardrail #5). make_scaler() returns UNFITTED; to_sequences(df, T, scaler) is a
      pure definition.

      Ordered pipeline: sort_dedupe -> segment(>3min) -> filter_d(+drop non-engaging
      segments) -> compute_speed -> trim_idle(2.5 m/s) -> flag impossible->NaN ->
      counters(impossible vs missing) -> resample to strict 10s grid + interpolate
      within segment + *_missing masks -> encode_heading(sin/cos) -> tag is_emergency
      -> filter_min_length(30). Feature vector [lat, lon, baroaltitude, velocity,
      vertrate, sin(hdg), cos(hdg), onground] + *_missing masks; dist_to_runway_m
      retained for Phase 5.

      Validated against cycle-3 (notebook 07 Part 3): Filter D 18,928/19,057 = 99.3%,
      2,598 gap-splits, n_imputed_impossible>0 cohort = 513 segments, 4 emergency
      segments, AE matrix NaN-free, strict 10s grid; 19,849 segments / 3.16M rows.
      27 passing tests (backend/tests/test_preprocessing.py) with 2 CRITICAL guards
      (interpolation never crosses a segment boundary; make_scaler UNFITTED).

      Refactor A1: shared geo/derivation helpers extracted to backend/core/geo.py +
      backend/core/derivations.py (leaf, no credentials); crud/opensky.py re-exports.
      ADRs D-010/D-008/D-009/D-006 amended 2026-06-01.
  eda:
    status: "in_progress"
    gate_semantics: "one_shot"
    passed_at: null
    artifact: "backend/docs/ml/04-eda.md"
    insights_count: 7
    summary: |
      Cycle-3 structural EDA across two rounds.

      Round 1 (2026-05-29): original 6 insights under the drone-detection
      framing. Filter C2 (landings-only) proposed as LEMD-operation gate.

      Round 2 (2026-05-30): post-Phase-1 architectural reframe triggered
      re-derivation. Filter D (three-criterion engagement gate) replaces C2
      under the behavioral-anomaly framing. Phase 0 phase-derivation
      validation added. Insight 7 (motion-feature normality audit) added,
      including 7d/7e/7f sub-investigations. Multi-detector preprocessing
      pipeline designed (physical-bounds rule + imputation + LSTM AE as
      separate detection channels). Sanity-cohort definition for D-008
      Layer 1 made concrete. All locked in D-010.

      Phase 4 stays in_progress (one_shot semantics) until D-008 Layer 4
      prep (OpenSky Dataset #6 LEMD-area inspection) lands. D-009 probe is
      a separate Phase 7 notebook. **Phase 3 close-out does NOT block on
      remaining Phase 4 deliverables** — the decisions surfaced here (Filter
      D, multi-detector pipeline, sanity cohort) are sufficient for Phase 3
      entry and are codified in D-010.
  features:
    status: "passed"
    gate_semantics: "one_shot"
    passed_at: "2026-06-01"
    artifact: "backend/docs/ml/05-features.md"
    feature_count: 9
    summary: |
      Phase 5 closed 2026-06-01 (issue #25). Feature work on the Phase-3 clean
      per-segment frames; firewall intact (no split, no scaler fit, no T — Phase 6
      on TRAIN only). Every feature leak-free (per-segment, fixed runway reference).

      - dist_to_runway_m PROMOTED to a scaled AE feature (AE_FEATURES 8->9,
        SCALER_FEATURES 5->6) — the nonlinear min-over-8-runways zone signal; the
        shared geometry (backend/core/geo.distance_to_closest_runway) the APW/geofence
        Layer-3 baseline + the zone/route synthetic injection both bind to. Rejected
        ENU x/y + bearing (redundant with lat/lon) and n_imputed-as-input (per-segment
        constant). The "dist helps the AE" claim is reasoned, tested empirically in the
        Phase-6 ablation.
      - apply_segment_derivations (backend/core/features.py): the shared derivation
        replay (hdg_sin/cos + dist from the measured primitives) — makes the injection
        bench's "perturb measured -> replay -> window+scale" structural. Footgun
        documented + test-locked: perturbing a derived channel directly is silently
        reverted.
      - Go-around held-aside cohort meta['is_go_around'] = 191/19,849 segments (0.96%),
        single-airborne-run descend-then-climb rule (eng-review caught + fixed a
        touch-and-go misclassification in the first global-argmin version). Routed OUT
        of TRAIN at the Phase-6 split (D-009 amendment); a high-precision real-anomaly
        validation cohort alongside Olive-7700 (D-008 Layer-4 companion).
      - Loss stays equal-weighted (carry to P6); per-feature RE is a diagnostic only.

      Code: backend/core/features.py (+ contract in preprocessing.py). Tests: 43 total
      (27 preprocessing + 16 features). Reconciliation docs (07-eval-prep, D-011, sadar
      bench header) updated for the AE 9 / SCALER 6 contract bump.
  train:
    status: "passed"             # Phase-6 work complete; see concern in summary + 07-train.md
    gate_semantics: "one_shot"
    passed_at: "2026-06-02"
    artifact: "backend/docs/ml/07-train.md"
    track_confirmed: false       # PROVISIONAL: AE beat IF (0.664 vs 0.625) but a wider panel
                                 # (kNN-on-summary 0.707) BEATS the AE on synthetic val. Track
                                 # decision DEFERRED to the Phase-7 real-anomaly test; AE + frozen
                                 # kNN both carried. model_track stays "dl" tentatively. (§4c)
    baseline_score: "IF val AUROC 0.625 [0.611, 0.639] (synthetic-injection val)"
    best_model_id: "lstm_ae_small_h32_l16_1L_mean (backend/models/phase6/lstm_ae_best.pt)"
    fitted_pipeline_artifact: "backend/models/phase6/{lstm_ae_best.pt, split_ids.json}; scaler refit-on-train(seed 42); T=260; threshold 0.222"
    summary: |
      Phase 6 closed 2026-06-01, codex-reviewed + corrected 2026-06-02 (issue #27). Split
      DEFINED + TEST(2020) SEALED; firewall intact (burned=false). 19,849 segments → clean
      folds train 8,924 / val 5,942 / test 4,285 (sealed) / held-aside 698; block counts
      {9338,6073,4438} match the locked table. Scaler + T=260 fit on TRAIN only.

      Bake-off on §6 synthetic val injections (Generator A, 5 single-trajectory types;
      multi_drone deferred): IF baseline val AUROC 0.625 [0.611,0.639]; LSTM-AE best
      (small h32/latent16/1L, mean scoring) val AUROC 0.664 [0.651,0.678]. D-006 margin
      +0.040 (non-overlapping CIs) → ship AE; model_track=dl confirmed. Threshold-on-val
      0.222 → F2 0.451, FPR 0.149 (≤15% guardrail holds), PR-AUC 0.698.

      CODEX REVIEW (2026-06-02, GATE PASS): 2 P2s found+fixed+test-locked — injection onset
      could land past T (label glitch), and the LSTM encoder ingested padding (~95% of
      segments). Fixing the encoder removed a padding artifact that had inflated big/topk to
      0.684; post-fix the whole config grid is FLAT 0.646–0.664, so capacity/scoring don't
      matter and the simplest config (small/mean) ships. Headline 0.684→0.664 = more honest,
      not a regression. dist_to_runway_m ablation Δ +0.003 (neutral; retained for geofence
      baseline + zone injection). Seen-vs-unseen-icao24 delta −0.066 (recurrence-optimism
      refuted; eng-review-2 + Codex).

      WIDER BASELINE PANEL (§4c, nb12; codex + eng-review validated): D-006 only compared AE vs
      IsolationForest. A kNN-on-summary density baseline (same firewall) scores 0.707 > AE 0.664
      > IF 0.625 — the deep model does NOT earn its complexity on synthetic. "Ship AE / dl
      confirmed" is therefore PROVISIONAL (track_confirmed=false). kNN added as
      core/baseline.KNNSummaryBaseline + frozen (models/phase6/knn_train_summary.npy + scaler.joblib);
      AE + kNN both carried, blind, into the Phase-7 real-anomaly burn (07-eval-prep Layer 6) —
      real anomalies decide. The position-density "corridor" idea does NOT rescue zone (0.57).

      CONCERN: best val AUROC 0.664 is below the Phase-1 primary target (AUROC > 0.85). Loop-back
      DONE — all 4 levers (longer schedule, ENU x/y, per-phase, per-type) run blind on VAL; NONE
      beat baseline meaningfully, and the post-fix flat grid confirms config is immaterial. The
      per-type read shows this is a benchmark/ARCHITECTURE property, not under-tuning: the AE caps
      on subtle spatial anomalies (zone 0.56 @40% — these belong to the geofence Layer-3, not the
      AE) and excels on dynamic ones (loiter 0.95, intercept 0.79) — its intended complementary
      role (D-008/D-010). small/mean ships. Phase-7 entry can proceed with honest per-type framing
      (lead with the AE-vs-rules table, not the 0.66 mean); the SADAR real-anomaly head-to-head (07-eval-prep
      Layer 6) is the contest that matters. Firewall-semantics change = D-009 implementation
      amendment. See 07-train.md §4b + "Phase-7 entry decision".
  eval:
    status: "passed"
    gate_semantics: "one_shot"
    passed_at: "2026-06-02"
    artifact: "backend/docs/ml/07-eval.md"
    holdout_score: "AE synthetic test AUROC 0.731 (D-012 mix); AE real-anomaly test ROC 0.667"
    holdout_ci: "AE synthetic AUROC 95% CI [0.714, 0.745] (1000x bootstrap on test windows)"
    track_confirmed: true   # ship the LSTM-AE (dl). DECIDED on the pre-registered real-anomaly
                            # criterion (07-eval-prep Layer 6): AE real-anomaly ROC/PR BEATS the
                            # kNN (0.667/0.088 vs 0.595/0.067 on test-normal; 0.591/0.048 vs
                            # 0.551/0.043 on val-normal) — the kNN's SYNTHETIC win (0.786 vs 0.731
                            # test) does NOT transfer to real go-arounds/emergencies. Honest caveat:
                            # primary AUROC>0.85 target NOT met (0.731); kNN stays a strong simpler
                            # alternative on synthetic. See 07-eval.md + D-012.
    summary_phase7: |
      Phase 7 closed 2026-06-02 (issue #29). TEST BURNED ONCE (test_set.burned=true). Three
      models scored, all reported (user-amended Layer-6: report all, drop none):

      SYNTHETIC (D-012 re-weighted mix, zone OUT of headline — APW owns it; kept as diagnostic),
      sealed 2020 test fold: AE AUROC 0.731 [0.714,0.745] / PR 0.770; kNN 0.786 / 0.827;
      IF 0.717 / 0.734. kNN > AE on synthetic (as on val). AE op-point (thr 0.222, val-chosen,
      no retune): F2 0.520, FPR 0.089 (<=0.15 guardrail HOLDS), recall 0.474. Per-type (test):
      zone 0.551 (out-of-remit), altitude 0.558, loiter 0.971, intercept 0.789, speed 0.580
      (kNN 0.815). AE owns the DYNAMIC conformance anomalies (loiter/intercept) — its D-010 remit.

      REAL-ANOMALY (Layer 6, the contest that matters), held-aside emergency∪go_around (195 segs)
      vs 2020-test-normal (SADAR-comparable): AE ROC 0.667 / PR 0.088; kNN 0.595 / 0.067;
      IF 0.495 (chance). SADAR VAE-LSTM (native, his 2020-normal, reproduced exactly): 0.659 /
      0.299. AE > kNN on real (representative confirmed); AE ROC 0.667 EDGES SADAR 0.659 on the
      prevalence-invariant lens. PR gap (0.088 vs 0.299) is prevalence/windowing (4.4% whole-seg
      vs 12.1% sliding-window), not model quality — stated as a confound, not buried.

      LAYER 4 (external, OpenSky #6 7700): BCS63A — the one genuine close-in LEMD emergency
      turn-back (the other 6 in-range flights are transit, excluded by Filter B) — scored at the
      98.8th pctile (AE) / 100th (kNN) of normal. Real emergency flags high. N=1 case study (the
      doc's reframe; quantitative external role is D-011, deferred).

      LAYER 5 (qualitative top-20 normal-val by AE error): long full-profile segments; 7/20 show
      circling/holding signatures, several oscillating-vertrate; 3 are AE-flagged but kNN-normal
      (order-abnormal yet summary-normal — what the AE's sequence model catches). Final visual
      classification is the writeup owner's pass.
    summary: |
      Phase 7 entry decision pending. Two artifacts feed this gate:

      1. backend/docs/ml/07-eval-prep.md — synthetic anomaly injection
         calibration (Layer 2 of D-008 validation stack). Research synthesis
         from FAA UAS Sightings + Bard + Spanish news, ~2,200 incidents,
         2026-05-10. Headline changes: asymmetric upward altitude shift
         (not symmetric ±300m), sustained-loiter variant (60-300s alongside
         30s), demoted speed spike (1.5-2x, ≤10% of mix), 40%
         zone-violation share, add final-approach-intercept and multi-drone
         types.

      2. D-008 — multi-layer output validation (added 2026-05-23). The
         reconstruction-error number has no semantic meaning until external
         evidence grounds it. Validation stack: (1) sanity, (2) discriminative
         AUROC on synthetic injections per D-005, (3) realism via geofence +
         safety-net rules baseline, (4) **external** via OpenSky Dataset #6
         (Olive et al., 2020) — real 7700-squawk emergency flights the
         model never trained on or saw injected, (5) qualitative top-K
         review of model-flagged "normal" flights. Layers 4 and 5 protocols
         live in 07-eval-prep.md > "Validating beyond synthetic discrimination."
  deploy:
    status: "pending"
    gate_semantics: "one_shot"   # course-project demo, not a production deploy.
                                 # Would be "ongoing" for a real-time production system.
    passed_at: null
    artifact: null
    monitoring_dashboard: null
    summary: null

# THE TEST SET FIREWALL — sacred. Never peeked, never modified except in Phase 7.
#
# Split-discipline note (2026-05-31): a teammate's parallel project SADAR
# (huggingface.co/spaces/devrup404/sadar), on the SAME OpenSky-LEMD data, ships
# exactly the dual-criterion leak-free split we planned: jointly by flight_id
# AND by date — no flight appears in two splits (group leakage), and test is
# strictly later in time than train (temporal leakage). They use 2017-2019 for
# train/val and reserve 2020 for test, which doubles as a free distribution-shift
# stress test (start of COVID). This independently validates the strategy below.
# Caveat for us: our cycle-3 data is 18 *Mondays* spanning 2017-2020 (not
# contiguous, Monday-skewed per D-009), so a pure year-based temporal cut leaves
# a thin/uneven test fold. Decide in Phase 6 whether to mirror their year cut or
# use a group-only (flight_id/icao24) split with a held-out block of Mondays;
# either way, keep BOTH criteria (group + temporal) — that is the part to copy.
test_set:
  defined_at: "2026-06-01"       # Phase 6, issue #27
  split_seed: null               # deterministic by construction (fixed Monday→fold map, no RNG)
  split_strategy: "temporal-by-Monday (train 2017-18 < val 2019 < test 2020) + no-identity-leak
    guard (icao24/flight_id ∉ AE_FEATURES); held-aside emergency∪go_around∪impossible pulled
    pre-split. icao24 cross-fold overlap reported (train∩test 406, all-3 274), NOT asserted-zero —
    leak-free because identifiers are not features; seen/unseen AUROC Δ −0.061 confirms benign
    (D-009 + eng-review-2 amendment)."
  holdout_path: "backend/models/phase6/split_ids.json"   # test = 4,285 clean segments (2020)
  count: 4285
  burned: true                   # BURNED 2026-06-02, Phase 7 (issue #29) — the one-shot
  burned_at: "2026-06-02"
  burn_reason: |
    Phase-7 final evaluation (backend/scripts/phase7_burn.py). Scored the sealed 2020 fold
    ONCE with all three models on the D-012 re-weighted synthetic mix (AUROC/F2/FPR/PR-AUC +
    bootstrap CI vs IF) AND on the real-anomaly cohort vs 2020-test-normal (Layer 6). No
    tuning, threshold, or model selection used the test fold — the AE threshold (0.222) and
    the representative choice (AE) were both fixed on val/held-aside before the burn. Results:
    07-eval.md + backend/models/phase6/phase7_burn_results.json.

# LLM-track sets unused (model_track is dl).
llm_sets:
  dev_set: { path: null, count: null }
  eval_set: { path: null, count: null, last_score: null, last_score_ci: null }

# Decisions log. Append-only.
decisions:
  - id: D-001
    phase: problem
    decided_at: 2026-04-11
    title: "Frame as anomaly detection, not three-class intent classification"
    path: backend/docs/ml/decisions/D-001-anomaly-vs-classification.md
  - id: D-002
    phase: problem
    decided_at: 2026-04-11
    title: "ADS-B as training modality; source-agnostic model at inference"
    path: backend/docs/decisions/README.md
  - id: D-003
    phase: problem
    decided_at: 2026-04-11
    title: "OpenSky Network as primary dataset"
    path: backend/docs/decisions/README.md
  - id: D-004
    phase: problem
    decided_at: 2026-04-11
    title: "Anomaly scoring only; no trajectory prediction"
    path: backend/docs/decisions/README.md
  - id: D-005
    phase: problem
    decided_at: 2026-05-07
    title: "Metric stack: AUROC primary, F2 operational, FPR<=15% guardrail, PR-AUC sanity, all vs IF baseline"
    path: backend/docs/ml/decisions/D-005-metric-stack.md
  - id: D-006
    phase: problem
    decided_at: 2026-05-07
    amended_at: 2026-06-01   # Phase 3 EDA-informed reconfirmation (issue #22)
    title: "Architecture: LSTM Autoencoder primary, Isolation Forest baseline; alternatives considered and rejected"
    path: backend/docs/ml/decisions/D-006-architecture-and-baseline.md
  - id: D-007
    phase: data
    decided_at: 2026-05-23
    title: "OpenSky scientific dataset as primary cycle-3+ data source; Filter B for LEMD-flight gate"
    path: backend/docs/ml/decisions/D-007-opensky-scientific-data-source-pivot.md
  - id: D-008
    phase: eval
    decided_at: 2026-05-23
    amended_at: 2026-06-01   # Phase 3: Layer-1 cohort=513, go-around cohort, per-feature RE attribution (issue #22)
    title: "Multi-layer output validation including OpenSky Dataset #6 (7700-squawk real emergencies) as external set"
    path: backend/docs/ml/decisions/D-008-output-validation-layers.md
  - id: D-009
    phase: preprocess
    decided_at: 2026-05-23
    amended_at: 2026-06-01   # Phase 3: pre-committed hold-aside split rule (issue #22)
    title: "Training on cycle 3 alone (option B); cycles 1+2 retained as day-of-week covariate-shift diagnostic probe"
    path: backend/docs/ml/decisions/D-009-day-of-week-covariate-shift-probe.md
  - id: D-010
    phase: preprocess
    decided_at: 2026-05-30
    amended_at: 2026-06-01   # Phase 3 implementation: NaN handling, counter split, idle-trim, gap re-seg, resample, interpolate-not-derive, segment unit (issue #22)
    title: "Filter D as LEMD-operation gate; multi-detector preprocessing pipeline (Stage 1 physical-bounds rule + Stage 2 imputation + Stage 3 LSTM AE)"
    path: backend/docs/ml/decisions/D-010-filter-d-and-multi-detector-preprocessing.md
  - id: D-011
    phase: eval
    decided_at: 2026-06-01
    title: "Real-derived synthetic anomalies from Dataset #6 non-LEMD emergencies (onset-anchored maneuver transplant); firewall split non-LEMD→Layer2, LEMD-area→Layer4. Phase-7 stretch; feasibility gate verified (per-timestamp squawk)."
    path: backend/docs/ml/decisions/D-011-real-derived-synthetic-anomalies.md
  - id: D-012
    phase: eval
    decided_at: 2026-06-02
    title: "Phase-7 entry: re-weight zone_violation out of the synthetic bench (APW owns zone under D-010); zone kept as out-of-remit diagnostic; MIX_V1_WITH_ZONE preserved for Phase-6 reproducibility. Signed-off deliberate deviation."
    path: backend/docs/ml/decisions/D-012-phase7-zone-reweight.md
