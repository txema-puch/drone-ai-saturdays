# Phase 6 — Baseline/reference fitting

## Selected baseline

The product baseline is deterministic rules plus `approach_reference_v1`; it is a statistical
reference, not a classifier. The artifact is generated by
`backend/src/sadar/pipelines/fit_reference.py` from the historical train fold only.

- eligible fit attempts: 3,774;
- attempts contributing usable speed/vertical rows: 3,764;
- reference cells: 10;
- digest: `b485f747154ea8d84ba6b5c980501e3a22bca9caff40c41711de107b03496c56`;
- 2017/2018 maximum median shift: 0.0769 reference widths (limit 0.5, pass).

The immutable artifact lives at
`backend/src/sadar/approach/resources/lemd_approach_reference_v1.json`.

## Model-selection result

No new supervised model is selected. Independent review labels do not exist, so training a
classifier would manufacture targets or learn the rules it is supposed to improve upon. The
historical LSTM remains an optional, incompatible-unit research benchmark and cannot affect
status or priority.

## Gate result

Passed with the deterministic/reference baseline selected and supervised ML explicitly rejected
for this iteration pending independent labels and demonstrated incremental value.
